# NEXUS 项目 · AI Agent 开发工程师面试问答全解

> 用途：面试前逐节过一遍，背熟「数字速查表」，把每道题的「答案」用自己的话复述一遍。
> 原则：所有数字都能在仓库里找到来源（文末附「数字来源索引」），被追问时能当场指认文件。
> 项目定位口径：**面向研发 Agent 的版本化企业知识平台（RAG + 代码智能 + MCP）**。

---

## 0. 数字速查表（先背熟，面试高频引用）

| 类别 | 数字 | 备注 |
|---|---|---|
| 代码规模 | **287 个 Java 文件**（194 主 + 93 测试） | 主代码 22,129 行 + 测试 12,398 行 ≈ 3.5 万行 |
| 迭代 | **123 commits，13 个版本**（0.2.0 → 0.8.5） | 约 3 周（2026-07-23 → 08-13） |
| 测试 | **414 个测试全绿**，JaCoCo 行覆盖 **71.3%** | 分支 52.5%，指令 71.3%；门禁仅 35% |
| 对外接口 | **11 个 MCP 工具** + 45 个 REST 接口 | 63 项 MCP 契约测试全过 |
| 代码检索 Recall@1 | **64.0% → 93.6%（+29.6pp）** | 500 题全仓库评测，468/500 |
| 代码检索 Recall@10 | **99.6%（498/500）** | 符号命中 99.6%，文件命中 100% |
| MRR@10 | **0.764 → 0.9596（+0.196）** | 消融实验 A3 → E6 |
| 延迟 | P50 334ms / P95 436ms | 优化后不劣化 |
| 消融实验 | 185 条排序变化中 **157 条升至 Top1**、9 条掉 Top1→2-5 | 四轮评审后「零排名变化」 |
| 并行优化 | 语义标注 P95 **316ms → 108ms（-65.8%）** | 3 分支并行 + 顺序保持 |
| 评测集 | **700 题**（500 代码 + 200 文档）+ 18 文件 hard-negative | 语料 SHA-256 冻结 |
| 文档检索 vs LightRAG | 命中率 **73.5% vs 70.5%**，Recall@1 71.0% vs 57.0% | 200 题 |
| 四方代码对比 Recall@1 | NEXUS **93.6%** / understand-anything 80.4% / RAGFlow 34.0% / LightRAG 15.0% | 同题库同口径 |
| 四方代码对比 MRR@10 | NEXUS **0.9596** / 0.8348 / 0.4609 / 0.225 | — |
| 可靠性 | **4 态降级语义 + 15 个稳定 warning code** | REST/SSE/MCP 三入口一致 |

---

## 1. 项目定位与背景

### Q1.1 用一句话介绍你的项目

**参考答案：**
> NEXUS 是一个面向研发团队的版本化企业知识平台。它把分散的产品需求文档、Git 代码仓库和测试信号，统一索引成"按项目 + 版本"组织的知识服务：人类团队通过 Wiki 浏览结论，编码 Agent（Cursor / Codex / Claude Code）通过 MCP 拿到带稳定证据编号、可回查、版本隔离的结构化上下文。

**要点**：一上来就点出「人 + Agent 双消费」和「证据可验证」，这把你和普通"文档问答 RAG"区分开。

### Q1.2 项目要解决什么业务痛点？

**参考答案：**
1. **编码 Agent 拿不到可信上下文**：LLM 直接读仓库要么超上下文、要么幻觉。Agent 需要的是"带编号、可回查、版本正确"的精确证据，而不是"看起来对"的模型空话。
2. **版本污染**：业务系统多版本并存，回答 v1 问题时绝不能被 v2 内容静默污染。
3. **结论不可核验**：多数文档 RAG 能回答问题，却说不清答案来自哪个版本、哪个项目、哪条证据。
4. **需求/代码/测试三张皮对不上**：产品文档、代码实现、测试用例之间经常矛盾，需要冲突检测。

**加分点**：可以举一个具体例子——"某功能在 v1 和 v2 流程不同，Agent 如果拿到 v2 的证据去改 v1 的代码就会改错；NEXUS 通过 projectId + version 双重过滤杜绝这一点。"

### Q1.3 为什么不做成纯聊天机器人，而要做 MCP + Wiki 双形态？

**参考答案：**
- **人类团队**需要可浏览、可审核、有版本历史的 Wiki（结论要经过"草稿 → 人工审核 → 发布"）。
- **编码 Agent**需要的是**结构化、可调用、带引用**的工具接口，而不是自由文本聊天。
- 两者共享同一套检索管线和证据体系，保证"人看的"和"Agent 用的"是同一批事实，不会各说各话。

### Q1.4 架构总览（分层讲）

**参考答案（按 README 的 5 层）：**
1. **摄入层**：需求文档（Tika 解析 Excel/ZIP/HTML）、代码扫描（Tree-sitter）、可选测试快照。
2. **存储层**：Qdrant（向量）、SQLite（符号图）、文件型 Wiki/档案/草稿。
3. **检索层**：`RetrievalPipeline` 统一编排——路由、混合检索、限流、降级、告警。
4. **知识层**：Wiki 生成、版本对比、草稿生命周期、冲突分析。
5. **访问层**：REST + SSE + MCP；身份权限由外部统一网关管理。

**追问准备**：能画出"需求文档 → 分块 → 向量化入 Qdrant → 检索 → 引用白名单 → MCP 返回"的完整数据流。

### Q1.5 和市面上普通 RAG 产品的核心差异是什么？

**参考答案（对照表，讲 3 条即可）：**
| 维度 | 普通文档 RAG | NEXUS |
|---|---|---|
| 版本 | 无或弱 | projectId + version 双重隔离 |
| 证据 | 返回文本块 | 返回 `requirement:*`/`code:*` 稳定编号 + 引用白名单 |
| 降级 | 搜不到就乱答 | 明确区分"没搜到(NO_RESULTS)"和"搜不了(FAILED)" |
| 代码 | 只索引文本 | Tree-sitter AST + 符号图 + 影响分析 |
| 交付 | 网页问答 | Wiki（人）+ MCP（Agent）双形态 |

---

## 2. RAG 与检索管线

### Q2.1 讲讲你的检索管线是怎么设计的？

**参考答案：**
`RetrievalPipeline` 是统一入口，流程是：
```
QueryRouter 路由 → 混合检索（dense + desc_dense + sparse 三路）
  → RRF 融合 → BGE 重排（可跳过）→ LLM 精排（可跳过）
  → 引用白名单 → Top K 返回
```
- 需求文档走 Qdrant 混合检索 + BGE 重排；
- 代码走 `CodeKnowledgeService`，有独立的精确符号通道和结构化重排；
- 全链路带熔断、有界线程池（总超时取消）、缓存（键含配置指纹）。

### Q2.2 什么是混合检索？为什么比纯向量检索好？

**参考答案：**
- **纯向量（dense）**擅长语义相近但词面不同的查询（"怎么退款"能召回"退款流程"）；
- **纯关键词（BM25/sparse）**擅长精确标识符、术语、编号（如 `VipMoaServiceImpl`、`orderId`）——这些在向量空间里常常不敏感；
- 我的做法是 **dense + sparse 两路召回后做 RRF 融合**，互补短板。代码场景还加了第二个 dense 向量 `desc_dense`（业务语义描述向量），构成**三路**。

**追问「为什么代码检索要双向量」**：代码的原始文本是符号和实现，但用户查询常常是中文业务描述（"VIP 特权哪里实现"）。所以我把代码块额外生成一条"中文业务语义描述"的向量 `desc_dense`，让它能对上中文查询，同时又保留原文 dense 对符号的召回。这是代码 RAG 和文档 RAG 的关键差异。

### Q2.3 RRF 是什么？怎么融合多路排序？

**参考答案：**
RRF（Reciprocal Rank Fusion）对每路检索结果按排名给分 `score = Σ 1/(k + rank)`（k 通常 60），再把多路分数求和排序。它不需要做分数归一化（不同检索器的分数不可直接比），只依赖排名，稳健且可解释。

**追问「为什么不直接加权拼接分数」**：dense 余弦相似度、BM25 分数、稀疏点积分数的量纲完全不同，硬加权需要大量调参且脆弱；RRF 只看排名，鲁棒。

### Q2.4 稀疏向量（sparse）是怎么生成的？

**参考答案：**
用 `SparseVectorizer` 做词项权重（如 BM25 式 tf-idf 或词频），生成稀疏向量存入 Qdrant 的 sparse 字段，配合 dense 做混合检索。中文场景会先做分词（CamelCase 拆词 + 中文二元/三元组）。

**追问「中文代码查询的分词难点」**：中文无天然空格，我用了二元组/三元组（bigram/trigram）切分，同时对英文标识符做 CamelCase 拆分（`VipMoaServiceImpl` → `vip/moa/service/impl`），保证符号名能被命中。

### Q2.5 Parent-Child 分块是什么？解决了什么问题？

**参考答案：**
- 普通固定窗口分块会把一个"父概念"切成多个孤立的 chunk，检索时可能命中"父块里的答案文字"但返回"不含答案的子块"。
- 我用了 **parent-child 两级分块**：先切大父块（如一个章节/功能），再在父块内切小子块。检索用子块（精确），返回时能带出父块上下文；排序上做了"代表子块优化"——同一个父块只保留最相关的子块代表，避免父子混合污染排序。

**追问「你当时踩的坑」**：评测发现一类失败 `DOCUMENT_PARENT_AGGREGATION_LOSS`——正确子块进了 BGE 结果，但最终父块聚合时保留了同父块的另一个子块。我加了两个阈值（绝对分差 ≥0.01 且相对提升 >10% 才替换代表）修复，Child Recall@1 从 79.2% 提升到 83.3%、严格失败从 2/24 降到 0/24。

### Q2.6 重排（rerank）怎么做的？为什么两级？

**参考答案：**
两级重排：
1. **BGE 重排**（`bge-reranker-v2-m3`，独立 Python 服务）：对混合检索候选做交叉编码器精排，比向量相似度准很多；
2. **LLM 精排**（可选）：对 BGE 结果再做语义精排。
- 成本优化：配置 `llm-rerank-skip-gap`，当 BGE top1 与后续候选分差超过阈值时**跳过 LLM 重排**，省 token 省延迟。

**追问「BGE 挂了怎么办」**：降级——BGE 不可用保留原始候选并记 `BGE_RERANK_UNAVAILABLE` 警告；LLM 不可用回退 BGE/原始顺序。重排是"锦上添花"，绝不能因为重排挂了让整个检索失败。

### Q2.7 Embedding 遇到过什么工程问题？

**参考答案：**
- **连接半开挂起**：OpenAI 网关经 VPN 隧道，keep-alive 超时后半开连接导致 embedding 请求无限挂起 → 禁用 okhttp 空闲连接池复用，读超时 20s；
- **重试**：失败重试 5 次、2s 起指数退避；
- **批大小**：上游 `text-embedding-v4` 批上限是 8，我从 32 调到 8；
- **缓存指纹**：embedding 缓存键纳入向量维度指纹，模型/维度一变自动失效。

### Q2.8 检索结果缓存怎么设计的？

**参考答案：**
`BoundedTtlCache` 有界 + TTL 缓存检索结果，键**纳入配置指纹**（任何检索配置变更自动失效，避免返回旧配置的脏结果）。注意只缓存 `SUCCESS` 和 `NO_RESULTS`，**不缓存 `DEGRADED`**（降级结果是残缺的，不能复用以误导后续请求）。

### Q2.9 QueryRouter 是干什么的？

**参考答案：**
判断一次查询该走哪个项目（多项目部署时）。规则路由优先，LLM 路由兜底；LLM 路由不可用时回退默认项目并记 `ROUTING_LLM_UNAVAILABLE`，返回非法结果记 `ROUTING_INVALID_RESULT`。

---

## 3. 向量数据库 Qdrant

### Q3.1 为什么选 Qdrant 而不是 Milvus / FAISS / pgvector？

**参考答案：**
1. **原生支持 sparse + dense 混合检索**（这是我要的核心能力，pgvector 需要额外扩展）；
2. **Payload 过滤**（项目/版本/文件路径范围过滤，版本隔离靠它）；
3. **alias 原子切换**（代码索引的蓝绿发布靠它）；
4. 单机部署简单（Compose 一个容器），Rust 实现性能好，有 Prometheus 指标。

### Q3.2 代码索引的"蓝绿发布"（alias 原子切换）怎么实现的？

**参考答案：**
- 代码索引发布到**版本化物理 collection**（如 `code_<base>-v1`），完成后用 **alias 原子切换** `<base>-live` 指向新 collection；
- 校验失败**不切换**，保留最近两个版本 collection 用于回滚；
- 增量索引则用"文件级安全替换 + 最终一致"（滚动快照旧 chunk ID → upsert 新 ID → 只按旧 ID 删除，部分失败可重试收敛）。

**追问「为什么不全量原子切换」**：全量切换一致性强但成本高；增量路径用"按旧 ID 删除"保证**重试不会删掉刚写入的新 chunk**（删除前过滤本次 upsert 的新 ID）。代价是查询侧短期可能新旧并存（最终一致）。这个 trade-off 我明确写进了文档。

### Q3.3 遇到过的 Qdrant 兼容性坑？

**参考答案：**
- 点 ID 校验：Qdrant ≥1.13 拒绝 `$point_id` + `match.any`，改用 `has_id`；
- `swap_aliases` 在本机 1.15.4 上因 untagged enum 解析失败返回 400 → 回退 delete+create；
- alias 与遗留物理 collection 冲突自动清理后重试。

---

## 4. Agentic 检索编排

### Q4.1 你的"Agentic 检索"是什么？和普通 RAG 有什么区别？

**参考答案：**
普通 RAG 是"一次检索 → 拼接 → 生成"的单跳流水线。我实现了一个**「规划-检索-反思-补检」的多跳循环**（`AgenticOrchestrator`）：
1. **规划（选择策略）**：`RetrievalStrategySelector` 按画像+查询意图选首跳策略（需求评审画像 → 需求单线；代码意图查询 → 代码单线；否则走组合策略）；
2. **检索**：执行选中策略；
3. **反思（EvidenceReflector）**：对结果自评，输出三态裁决——`CONFIDENT`（证据充分，立即交付）/ `INSUFFICIENT`（证据不足，触发补检）/ `NOT_RETRIEVABLE`（核心失败，降级交付）；
4. **补检**：`INSUFFICIENT` 时用 `hybrid` 组合策略补检，与已有证据按值去重合并；最多 2 跳。

**追问「为什么最多 2 跳」**：第 0 跳已经是组合全查，补检只需 1 跳就能覆盖"需求不足补代码/代码不足补需求"的场景；更多跳数收益递减、延迟线性上升，还容易兜圈子。2 是经过权衡的默认值（可配置）。

### Q4.2 反思器（EvidenceReflector）怎么判断"证据够不够"？

**参考答案：**
规则版反射器看三类硬信号：**命中阈值**（召回分数/数量是否达标）、**唯一父块覆盖**（是否覆盖到具体证据块而非泛泛）、**双侧覆盖**（需要需求+代码的场景是否两侧都命中）。输出带**稳定 reason code**，可解释、可测试（不是让 LLM 自由发挥，避免反思本身成为新的幻觉源）。

### Q4.3 为什么不直接用 ReAct / LangGraph 这类框架？

**参考答案：**
1. 我的反思是**确定性的规则裁决**，而不是 LLM 每跳自由生成 action——检索场景需要的是可复现、可评测、低延迟，LLM 自由思考会引入不确定性；
2. 项目是 Spring Boot/Java 栈，引入 Python 的 LangGraph 会割裂技术栈、增加部署复杂度；
3. 我需要的多跳只有 2 跳，手写一个明确的状态机（策略池 + 选择器 + 反射器）比引一个通用框架更可控。这体现的是"理解原理后做减法"的判断。

**加分点**：可以说"如果未来要 5 跳、10 跳的动态工具编排，我会评估 LangGraph/Spring AI 的 Agent 框架，但当前 2 跳状态机完全够用且更好测"。

### Q4.4 策略池是怎么设计的？

**参考答案：**
策略是一个接口 `RetrievalStrategy`，有三个实现：
- `RequirementsOnlyStrategy`：只查需求文档；
- `CodeOnlyStrategy`：只查代码；
- `HybridCompositionStrategy`：需求+代码组合。

选择器按 profile + 查询意图关键词（实现/方法/接口/代码/如何/调用/类/函数/服务/impl/service/controller/how/implement/method 等）做**确定性匹配**，无匹配回退组合策略。这样"需求评审"和"代码定位"各自走最高效的单线，减少无关召回噪声。

---

## 5. 代码智能（Tree-sitter / 符号图 / 影响分析）

### Q5.1 代码是怎么被"理解"的？

**参考答案：**
分四层，**索引阶段零 LLM**：
1. **AST 解析**：Tree-sitter 多语言（Java/Go/Python/TypeScript，Kotlin 启动探测安全启用），解析出类、方法、调用关系；
2. **符号图**：SQLite 按项目 + Git commit 事务化存符号、调用边、解析置信度、未解析调用；
3. **语义标注**（可选 LLM）：为核心代码块生成中文业务描述、关键词、同义词（`desc_dense` 向量来源），三连败熔断 + 磁盘 JSONL 缓存避免重复调用；
4. **影响分析**：从起始符号做 BFS 遍历调用图，`EXACT`/`SAME_FILE` 计确定影响，`HEURISTIC` 计推断，动态分派/歧义显式 `UNRESOLVED`。

**追问「为什么索引不用 LLM」**：结构化符号靠解析器就 100% 准确且零成本；LLM 只用于"锦上添花"的语义描述，且缓存 + 熔断控制成本。对比 LightRAG 全仓库 LLM 抽取要数小时，我的 Tree-sitter 索引几分钟。

### Q5.2 精确符号通道（exact channel）是什么？这是 Recall@1 提升的核心吗？

**参考答案：**
是的，这是 +29.6pp 的主因。核心问题是：查询里已经带 `VipMoaServiceImpl.queryVipShopIndex` 这种接近唯一键的信息，但旧链路仍走概率检索，语义相似的类 chunk/同名方法会排在目标前面。

我做了三级：
1. **`CodeQueryAnalyzer` 确定性解析**（不调 LLM）：识别 `ClassName.methodName`、引号内方法名、"在 X 中实现"句式、显式文件路径，输出 `ParsedCodeQuery`（EXACT_SYMBOL / CLASS_SCOPED / GENERIC）；
2. **`SQLiteSymbolGraphStore.findExactSymbols`**：精确符号命中时按行范围直接从源文件确定性构建 chunk 置顶；SQLite 异常自动回退混合检索；
3. **结构化重排加分**（`CodeQdrantStore`）：类名命中 +0.80、方法名精确命中 +0.50、完整 `Class.method` +1.50、文件名与类名一致 +0.50；稳定排序键加 `exactMatchLevel`（2=类+方法 / 1=仅类 / 0=无）、filePath、startLine。

### Q5.3 消融实验怎么证明每一步都有效？

**参考答案：**
每个优化都有独立开关：`code-exact-symbol-enabled` / `code-class-scoped-enabled` / `code-structural-rerank-enabled`（默认开，纳入检索指纹）。跑基线 A（全关）→ 实验 B-F（逐项开）→ E6（全开）：
- 基线 A3：Recall@1 64.0% / MRR 0.764；
- 最终 E6：Recall@1 93.6% / MRR 0.9596（+29.6pp / +0.196）；
- 关键是"逐开关归因"——不是一把梭全开，而是能说清"精确符号通道贡献了多少、类名限定召回贡献了多少、结构重排贡献了多少"。

**追问「怎么证明没 overfit 到评测集」**：评测集是冻结的（语料 SHA-256 固定、gold 固定）；第三、四轮评审修复后**对评测集零排名变化**（E7 vs E6、E9 vs E7），说明指标稳定可复现；且消融实验本身就是在防止"调出高分但不知道为什么"。

### Q5.4 你诊断出的那个 `/build/` 排除规则的 bug 是怎么回事？

**参考答案：**
评测数据核查时发现 151 条排序失败中 97 条含完整 `Class.method`；进一步确诊 8 条查询（2 个目标）因默认排除规则 `/build/` 的**子串匹配误伤了包目录名为 `build` 的 127 个源码文件**（`com/.../build/...` 这种合法包名），导致这些文件根本没进索引。修复：从默认排除列表移除 `/build/` 并全量重索引。

**这是很好的"深度定位"故事**：讲清楚"先看数据 → 定位到规则级根因 → 修复 → 全量重索引验证"的完整闭环。

### Q5.5 影响分析为什么区分 EXACT / SAME_FILE / HEURISTIC / UNRESOLVED？

**参考答案：**
影响分析的可靠性取决于调用边的解析置信度：
- `EXACT`：确定性解析到的调用（如直接方法调用）；
- `SAME_FILE`：同文件内调用（置信度也高）；
- `HEURISTIC`：启发式匹配（可能误报，单独返回，不计入确定影响）；
- `UNRESOLVED`：动态分派/反射/接口多态，显式保留为"未解析"。

**追问「为什么不能都当确定」**：静态分析对 Java 的接口多态、反射、Spring 注入天然有盲区，把 HEURISTIC 当确定影响会让"影响面"虚高，误导 Agent 做过度修改。诚实地标注不确定性，比假装全知道更符合工程原则。缺失图谱时显式降级到文件级 diff。

---

## 6. MCP 与 Agent 集成

### Q6.1 MCP 是什么？为什么用它对接 IDE Agent？

**参考答案：**
MCP（Model Context Protocol）是 Anthropic 提出的开放协议，让 LLM 应用通过统一的"工具/资源/提示"接口访问外部上下文。我实现的是 **MCP Server**（Streamable HTTP 传输），让 Cursor/Codex/Claude Code 这些 MCP 客户端用同一套工具拿到 NEXUS 的知识。

**追问「MCP 和 Function Calling 的区别」**：Function Calling 是某个模型厂商的调用约定，MCP 是**跨厂商、跨客户端**的标准协议——写一次 Server，各家 IDE 都能用；且 MCP 有 Resources（资源模板）和 Prompts（提示模板）的概念，比纯 tool 更丰富。

### Q6.2 你的 11 个 MCP 工具分别是什么？设计思路？

**参考答案：**
1. `nexus_search_requirements` — 版本化需求证据检索；
2. `nexus_search_code` — 代码检索（带受控摘录）；
3. `nexus_get_source` — 读受控源码片段（行数封顶 200 行）；
4. `nexus_development_plan` — 生成带引用的开发方案；
5. `nexus_wiki_page` — 读已发布的版本化 Wiki 页；
6. `nexus_wiki_index` — Wiki 索引摘要（按类型/过期过滤）；
7. `nexus_version_diff` — 两版本间需求/代码/测试/Wiki 差异；
8. `nexus_code_graph` — 遍历静态符号调用图（入向/出向、深度 1-5）；
9. `nexus_impact_analysis` — 影响面分析；
10. `nexus_review_doubts` — 存疑评审；
11. `nexus_conflict_check` — 知识冲突检测。

**设计原则**：全部**只读**（不替代本地 Git）；返回**结构化 + 稳定证据编号 + 显式告警 + 截断状态**；统一响应策略（列表上限、源码行数上限、总字符数上限）。

### Q6.3 Streamable HTTP 和 stdio 怎么选？为什么两个都要？

**参考答案：**
- **Streamable HTTP**（`/mcp`）：远程共享服务，多客户端并发，适合团队部署；
- **stdio**：本地进程通信，适合单人本机 IDE，延迟低、无需网络。
- 我提供 Streamable HTTP 为主，另附一个固定版本的 stdio 兼容桥脚本，适配不同客户端的接入习惯。

### Q6.4 MCP 层怎么做安全？

**参考答案：**
1. **鉴权**：MCP 初始化及后续请求都校验 `X-API-Key`，工具调用继续走角色 + 项目白名单授权（ThreadLocal 用户上下文 + HTTP Filter）；
2. **仓库边界**：源码读取按真实路径校验，**拒绝通过符号链接读取仓库外文件**；
3. **防泄露**：响应移除绝对路径与内部存储标识，审计日志不记录查询正文/API key/完整证据。

### Q6.5 63 项 MCP 契约测试测了什么？

**参考答案：**
六个核心工具 × 四类契约：**入参校验**（非法参数拒绝）、**鉴权/角色/项目白名单**（越权拦截）、**预期依赖不可用时的安全降级**（降级不崩）、**数量/文本/范围/全局响应截断**（防止超长响应打爆 Agent 上下文）。63 项全过，这是"Agent 工具必须有契约保障"的体现——工具给错了上下文比不给更危险。

---

## 7. 证据引用与防幻觉

### Q7.1 RAG 为什么会"看似正确但引用错误"？你怎么防？

**参考答案：**
RAG 的幻觉来源：模型可能在生成时"脑补"证据、引用错误段落、或把相似文档的内容当成目标文档。我的三道防线：
1. **引用白名单（EvidenceRegistry）**：模型**只能引用本次请求实际召回的证据**，引用不在白名单里的编号会被过滤并告警；
2. **稳定证据编号**：每条证据生成 `requirement:*` / `code:*` 编号，用户能点开回查原文；
3. **Claim 质量门**：Wiki 的每条声明必须绑定对应类型的已注册证据，无证据/引用越界/跨项目/跨版本证据都**阻止发布**。

### Q7.2 稳定证据编号怎么设计？为什么不直接返回原文？

**参考答案：**
编号是"项目 + 类型 + 哈希"派生（如 `code:37c01bb4-...`），**稳定且不暴露本地绝对路径**。返回编号而不是裸原文，是因为：① Agent 需要能引用/追溯的最小句柄；② 长度可控（不撑爆上下文）；③ 换环境（不同机器路径不同）编号依然稳定可复现。

### Q7.3 开发方案（development plan）的引用是怎么嵌入的？

**参考答案：**
SSE 流式生成开发方案时，逐条把方案章节与召回证据做**双向匹配**（`PlanSectionEvidenceMatcher`）：每条方案 section 标注引用状态（SUPPORTED / 缺失 / 非法），非法引用被过滤，缺失引用显式告警，并输出**回答证据覆盖率**统计。让"AI 生成的方案"每句话都可追溯到证据。

---

## 8. 评测体系与消融实验

### Q8.1 你的评测集是怎么构建的？怎么保证可信？

**参考答案：**
1. **700 题**：500 代码题（4 类模板 × 125 目标，覆盖 BUSINESS_TERM / SYMBOL / BEHAVIOR / REQUIREMENT_TO_CODE）+ 200 文档题；
2. **hard-negative 语料**：18 文件语料，6 类干扰（同词异义、近似流程错阶段、同义跨文档、父块泄漏、跨文件近似重复、无结果），专治"评测集太简单导致假满分"；
3. **冻结与防泄漏**：语料 SHA-256 固定、gold 固定、禁写生产特例、评测器独立校验；
4. **口径严谨**：质量分母按唯一 case，延迟样本按执行次数；BGE 请求超时该次运行标记 infrastructure contaminated 并**拒发分数**（contamination 零容忍）。

**追问「为什么 Recall@10=1.0 反而不可信」**：v1 评测集是单文件、48 个 HIT 全指向同一文件，Top-10 必然命中，分数饱和无法区分排序质量。所以我重做成 v2：18 个独立文件（超过 Top-10 截断）、三层 rank（File/Section/Child）、hard-negative，分数才开始有区分度。这体现的是"质疑自己的指标"。

### Q8.2 你的核心指标口径是什么？Recall 和 MRR 的区别？

**参考答案：**
- **Recall@K**：Top-K 中是否出现 gold（文件+符号同时命中才算命中）；
- **MRR@10**：首个 gold 命中排名的倒数平均值（第 1 名记 1，未进前 10 记 0）——衡量**排序质量**，比 Recall 更严格；
- **nDCG@10**：按排名折扣的相关度；
- **P50/P95**：延迟分布。

**追问「MRR 不是 Mean Rank」**：MRR 越大越好，Mean Rank 越小越好，两者别混淆（这是评测新手常犯的错误）。

### Q8.3 讲一次你"怀疑自己指标"的经历

**参考答案：**
（就是 Q8.1 的 v1→v2 故事 + `/build/` bug）我发现自己 v1 的 Recall@10=1.0 是"语料饱和"的假象，于是重做 hard-negative 评测集；又在评测数据核查中发现 8 条查询因排除规则误伤 127 个文件。两次都是"指标好看 → 深挖 → 发现评测或实现缺陷 → 修复"，这说明我对待数字是审慎的，不会拿假高分糊弄。

---

## 9. 横向对比（LightRAG / RAGFlow / understand-anything）

### Q9.1 你为什么做四方对比？公平性怎么保证？

**参考答案：**
为了验证 NEXUS 的定位不是"自嗨"，我在**同一个 500 题代码检索集、同一判定口径（文件路径+符号名同时命中 Top-K）**下对比了 LightRAG、RAGFlow、understand-anything 和 NEXUS：

| 指标 | LightRAG | RAGFlow | understand-anything | **NEXUS** |
|---|---:|---:|---:|---:|
| Recall@1 | 15.0% | 34.0% | 80.4% | **93.6%** |
| Recall@10 | 46.6% | 78.6% | 92.0% | **99.6%** |
| MRR@10 | 0.225 | 0.4609 | 0.8348 | **0.9596** |

**公平性**：同一题库、同一 gold、同一"符号+文件同时命中"口径；语料同源（都是全仓库约 2139 文件）；断点续传 + 逐例诊断，结果可复现。

### Q9.2 为什么 LightRAG / RAGFlow 的代码召回差这么多？

**参考答案：**
- **LightRAG** 是图谱 RAG，擅长文档级知识关系，但它的候选是"文本 chunk"，符号命中率虽高（84%）却**文件定位能力弱**（只有 62.6%），符号+文件同时命中就暴跌到 61.4%——它不区分"同名方法在不同文件"；
- **RAGFlow** 是文档解析 RAG，向量+关键词混合，但缺乏**结构化符号表**，无法区分 `handle`/`init`/`get` 这类高频同名方法的具体归属文件；
- **NEXUS 的优势是结构化定位**：Tree-sitter 符号图 + 精确符号通道 + 文件范围过滤，能靠符号表区分同名方法。

### Q9.3 understand-anything 是什么？和你的定位差在哪？

**参考答案：**
understand-anything 是"代码理解"工具，用 LLM 生成知识图谱（10077 节点 / 27601 边）+ 14 步中文导览，配套 `codebase-memory-mcp` 用 BM25 检索。
- 它的 Recall@1 只有 80.4%（其 BM25 基线仅 69.0%），且**知识图谱只覆盖 83.2% 的 gold 符号**（缺失 84 个）——LLM 建图天然有遗漏；
- 它**延迟极低**（P50 24ms，内存 BM25），NEXUS 是 334ms（向量+重排）；
- **定位差异**：understand-anything 是"给人类读懂仓库的导游式图谱"，NEXUS 是"给 Agent 精确检索 + 证据引用的上下文服务"。所以比精确召回是 NEXUS 的主场，比延迟是 understand-anything 快。

**追问「延迟输了怎么办」**：诚实承认——精度是用延迟换的（+13.2pp Recall@1 换 ~14 倍延迟）。但如果要在线低延迟，我有明确的优化路径：关闭 agentic 补检、分级重排（BGE 分差大就跳过 LLM）、精确符号查询走确定性通道（不走重排）。这是"知情权衡"而非"没意识到"。

---

## 10. 可靠性工程（降级 / 熔断 / 超时）

### Q10.1 什么是四态降级语义？为什么这么设计？

**参考答案：**
检索结果四态：`SUCCESS`（正常命中）/ `NO_RESULTS`（真零命中）/ `DEGRADED`（非关键依赖失败但保留候选）/ `FAILED`（核心依赖失败且无证据）。核心原则两条：
1. **依赖故障绝不伪装成空命中**——搜不到和搜不了是两回事，混淆会误导 Agent 做出错误判断；
2. **真零命中绝不返回 5xx**——空结果是合法结果，不该报错。

HTTP 映射：FAILED → 503 + `outcome=FAILED`；DEGRADED → 2xx + warnings；NO_RESULTS → 2xx + `status=NO_RESULTS`。

**追问「为什么不统一 500」**：503 是可重试的"服务暂不可用"，语义明确；把"没搜到"也报 5xx 会让调用方误以为系统挂了去重试，白白浪费。清晰的状态语义是给 Agent 的"信号契约"。

### Q10.2 warning code 注册表是什么？为什么要有 15 个稳定 code？

**参考答案：**
每个失败/降级路径登记一个稳定 warning code（如 `BGE_RERANK_UNAVAILABLE`、`ORCHESTRATION_INSUFFICIENT_EVIDENCE`、`MCP_RESPONSE_TRUNCATED`），REST/SSE/MCP 三入口一致映射。好处：
1. Agent 可以**程序化判断**"重排挂了但检索还在"而非解析自然语言错误；
2. 新增失败路径必须登记、禁止复用语义不同的 code，防止语义漂移；
3. 对外 warning 不含底层异常原文/密钥/URL 内部细节/源码全文（防泄露）。

### Q10.3 熔断器（Circuit Breaker）怎么用？

**参考答案：**
检索链路外部依赖（LLM 标注、BGE 等）用熔断器：连续失败达到阈值（如 3 次）打开熔断，短路后续调用直接降级，避免雪崩；恢复期半开试探。`CodeSemanticAnnotator` 的"三连败熔断"就是典型——标注 LLM 连挂 3 次就跳过标注走静态降级，不拖垮整个索引。

### Q10.4 超时和线程池怎么控？

**参考答案：**
- 检索用**有界线程池**（retrievalExecutor），跨项目检索设总超时，超时取消未完成 future（防止一个慢项目拖死全局）；
- Embedding 读超时 20s + 重试 5 次；
- SSE 流式设 STREAM_TIMEOUT，防止长连接泄漏。

---

## 11. 性能优化

### Q11.1 你的性能优化成果是什么？

**参考答案：**
1. **并行化语义标注**：LLM 标注从串行改 3 分支并行 + 顺序保持，**P95 316ms → 108ms（-65.8%）**（`parallel-recall-benchmark.json`，10 次重复、通过 30% 提升门槛）；
2. **索引零 LLM**：Tree-sitter 纯解析，对比 LightRAG 全仓库 LLM 抽取数小时，我的索引分钟级且零 token 成本；
3. **缓存**：标注结果磁盘 JSONL 缓存（按 chunk 哈希去重）、检索结果缓存（键含配置指纹）、embedding 缓存（键含维度指纹）。

### Q11.2 并行化怎么保证顺序和正确性？

**参考答案：**
并行标注用"顺序保持"策略：结果按输入顺序收集（索引映射回原位），避免并行乱序污染后续；基准测试用 controlled-fake-dependency 做延迟对比，保证对比的是真实并行收益而非假依赖噪声。

---

## 12. 工程化与质量

### Q12.1 你的测试策略是什么？414 个测试怎么来的？

**参考答案：**
分层测试：
- **单元测试**：策略选择器、反射器、分块器、重排、熔断、缓存等纯逻辑；
- **契约测试**：MCP 63 项、检索状态语义、降级路径；
- **集成测试**：MCP HTTP 集成、评测 setup；
- **质量门禁**：`RetrievalQualityGateTest` 冻结评测集黄金命中随 CI 默认回归（不依赖外部服务），保证检索优化不悄悄回退。

全量 **414 个测试全绿**，JaCoCo **行覆盖 71.3%**（门禁 35%，指令 71.3%，分支 52.5%）。

### Q12.2 你的 CI / 交付链路？

**参考答案：**
GitHub Actions（JDK 21 + `./mvnw -B verify`）、Maven Enforcer 强制 JDK 21 / Maven 版本、JaCoCo 覆盖率门禁、多阶段非 root Dockerfile、Compose 全家桶（NEXUS + Qdrant + Prometheus + Grafana + healthcheck）、`nexus.sh` 一键启停。

### Q12.3 可观测性怎么做？

**参考答案：**
Actuator + Prometheus（阶段级延迟直方图 `rag_stage_seconds_bucket`）+ Grafana + OTLP；告警规则（服务宕机、Qdrant 宕机、阶段 P95 > 60s、阶段失败率、空生成问题）；MCP 调用指标 + 安全审计日志（不记录查询正文/密钥）。

---

## 13. 版本化与知识管理

### Q13.1 版本隔离怎么实现？为什么重要？

**参考答案：**
主键 `projectId + version + featureId`，检索时 payload 过滤强制 `documentId + version` 双字段匹配（缺任一返回空）。重要性：业务多版本并存时，Agent 查 v1 问题若混入 v2 证据会改错代码。我的原则是"回答 v1 时不得静默使用 v2 内容"。

### Q13.2 "草稿 ≠ 已发布"是什么意思？

**参考答案：**
自动构建的 Wiki 只是**草稿/待审核**，必须人工审核才能发布为正式内容。`ModuleClaimQualityGate` 在发布前做质量门（无证据/引用越界/跨项目/跨版本证据阻止发布）。这防止"AI 自动生成的结论被当成已确认的产品事实"。

### Q13.3 过期检测（staleness）怎么做？

**参考答案：**
`WikiStalenessService` 只读计算、不覆盖已发布内容：基于 Git commit（`git rev-parse HEAD` + 文件级 diff 命中页面代码入口）+ 需求内容哈希对比，检测 Wiki 页是否过期；模块页支持 stale-to-draft 重建，只让审核人审变化声明（ADDED/MODIFIED/REMOVED/UNCHANGED），省审核成本。

### Q13.4 冲突检测检测什么？

**参考答案：**
`KnowledgeConflictService` 做确定性冲突分析，输出四类：**版本污染**（跨版本证据混用）、**项目污染**（跨项目证据）、**来源间冲突**（需求 vs 代码 vs 测试 vs Wiki 互相矛盾）、**Wiki 证据缺口**（声明缺证据）。原则是"检测并显式报告冲突，绝不自动仲裁伪装成一致"。

---

## 14. 开放性与深度追问

### Q14.1 如果让你重做，哪些地方会改？

**参考答案（展示反思能力）：**
1. **多实例索引协调**：当前索引锁是进程内（`ConcurrentHashMap` + synchronized），多实例部署会并发写同一 alias——应该引入分布式锁或 staging collection + 原子切换；
2. **文档检索延迟**：文档侧 14.5s 明显慢（BGE 重排 + agentic 补检 + LLM），应做分级重排和补检开关；
3. **更大评测门禁**：评测集还应扩展到多项目、多语言，CI 加质量阈值自动拦截回退；
4. **真实测试快照接入**：目前缺真实 CI 测试结果，界面只能显示"没有真实执行快照"。

### Q14.2 你项目最大的技术难点是什么？怎么解决的？

**参考答案（二选一重点讲）：**
1. **代码检索 Recall@1 从 64% 到 93.6%**：难点不是"上向量库"，而是发现"带唯一键信息的查询仍在走概率检索"这个根因，并用"精确符号通道 + 消融归因"系统解决（详见 Q5.2/Q5.3）。
2. **评测可信度**：发现自己 Recall@10=1.0 是语料饱和假象，重做 hard-negative 评测（详见 Q8.1）。

### Q14.3 动态分派/反射调用解析不了怎么办？

**参考答案：**
静态分析对 Java 接口多态、Spring 注入、反射天然有盲区。我的做法是**显式保留 UNRESOLVED**，影响分析时只把 EXACT/SAME_FILE 计入确定影响，HEURISTIC 单独标注。承认边界、不假装全知道，比强行解析更可靠。未来可结合运行时 trace 补充。

### Q14.4 如果要支撑更大的团队/仓库规模，瓶颈在哪？

**参考答案：**
1. 单 JVM 索引协调（见 Q14.1）；
2. 单一 Qdrant 实例的容量/吞吐（可做分片、多副本、按项目分 collection）；
3. 评测集需要多项目覆盖 + CI 质量阈值；
4. 需要共享仓库同步（目前要手动对齐 projectId 与已索引 commit）。

### Q14.5 你的方案里 LLM 都用在哪些地方？怎么控成本？

**参考答案：**
按任务路由（开发计划 gpt-5.6-sol、存疑评审 glm-5.2、代码标注 gpt-5.6-sol、重排 claude-sonnet-4.6）。控成本手段：索引零 LLM（Tree-sitter 解析）、标注分层（核心走 LLM、非核心走静态）、标注结果磁盘缓存 + 熔断、BGE 分差阈值跳过 LLM 重排。

### Q14.6 你觉得这个项目和"AI Agent"岗位的契合点在哪？

**参考答案：**
1. **Agent 上下文供给**：MCP + 证据引用 + 版本隔离，正是"让 Agent 拿到可信上下文"；
2. **Agent 编排**：agentic 检索的规划-反思-补检循环，是 Agent 工程的核心范式；
3. **Agent 工具可靠性**：契约测试 + 降级语义 + 截断策略，是"工具不能坑 Agent"的工程实践；
4. **评测闭环**：消融 + 硬负样本 + 横向对比，是 Agent 效果可量化、可迭代的方法论。

---

## 15. 电梯陈述模板（30 秒 / 2 分钟两版）

**30 秒版：**
> 我独立开发了 NEXUS，一个面向研发 Agent 的版本化企业知识平台，用 RAG + 代码智能 + MCP 把需求文档和代码仓库组织成"带证据编号、版本隔离"的知识服务。核心成果是代码检索 Recall@1 从 64% 优化到 93.6%（+29.6pp），在同口径评测中全面领先 LightRAG、RAGFlow、understand-anything；整个项目 3.5 万行 Java、414 个测试全绿、行覆盖 71.3%。

**2 分钟版（STAR）：**
> S：编码 Agent 需要可信的代码/需求上下文，但普通 RAG 说不清答案来自哪个版本、哪条证据，还容易幻觉。
> T：做一个版本化、证据可验证、能通过 MCP 喂给 IDE Agent 的知识平台，并把检索质量做到可量化领先。
> A：Java 21 + Spring Boot + Spring AI + Qdrant 全栈实现；检索用双向量 + sparse 三路 RRF 融合 + BGE/LLM 两级重排；代码用 Tree-sitter + SQLite 符号图，做了确定性精确符号通道；设计了 agentic 检索编排（策略池 + 反思器 + 2 跳补检）；证据白名单防幻觉；四态降级语义。
> R：代码检索 Recall@1 64%→93.6%（+29.6pp）、MRR 0.9596、Recall@10 99.6%；四方对比全面领先；414 测试全绿、71.3% 行覆盖。

---

## 附录 A：数字来源索引（面试前核对用）

| 数字 | 文件 |
|---|---|
| Recall@1 93.6% / MRR 0.9596 | `target/fengshen-retrieval/nexus-code-report-E6.json`（最新 `nexus-code-report.json`） |
| 消融 A3→E6 全过程 | `docs/code-recall-at1-improvement-plan.md` |
| 四方对比（LightRAG/RAGFlow） | `docs/fengshen-code-retrieval-three-way-comparison.md`、`docs/lightrag-nexus-fengshen-code-benchmark.md` |
| understand-anything 对比 | `/Users/user/Desktop/ua/fengshen-code-retrieval-eval-500-mcp-20260813.md`（BM25 基线同名 standalone 文件） |
| 文档检索 73.5% vs 70.5% | `docs/lightrag-nexus-fengshen-benchmark.md` |
| 414 测试 / 71.3% 覆盖 | `target/surefire-reports/`、`target/site/jacoco/jacoco.csv` |
| 并行化 P95 -65.8% | `target/retrieval-evaluation/parallel-recall-benchmark.json` |
| hard-negative 评测 v2 | `docs/retrieval-evaluation-v2.md` |
| 四态 + 15 warning code | `docs/retrieval-status-contract.md` |
| 版本迭代历史 | `CHANGELOG.md` |

---

## 附录 B：一句防坑提醒

- 所有"对比"数字要强调**同题库、同口径**，不要写成"同一次实验"（LightRAG/RAGFlow 是 08-12、understand-anything 是 08-13、NEXUS 优化到 08-14）。
- 延迟比 understand-anything 慢是事实，主动讲"精度换延迟"的权衡，别等面试官发现。
- 9 条从 Top1 掉到 2-5 位要会解释（类内语义排序的代价，全部仍在 Top5）。
- "独立开发"照你的口径，投实习时建议写"独立负责/主导"并保留真实主体，避免背调风险。
