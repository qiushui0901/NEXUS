# 实施计划

## 1. 共享基础设施

- [x] 建立设计 Token、应用外壳、组件和响应式 CSS。
- [x] 实现统一导航、移动菜单、上下文参数传递和连接设置弹层。
- [x] 实现共享 API 客户端、错误规范化和通知管理。
- [x] 扩展稳定状态词汇、图标与显示映射。
- [x] 增加共享资源与五个核心页面加载契约测试。

## 2. 知识库与 GitLab

- [x] 迁移知识库和 GitLab 到统一外壳与组件。
- [x] 将统计、筛选、状态、空/错/加载状态统一。
- [x] 增加 390px 结构化列表，消除横向表格依赖。
- [x] 优化知识详情阶段栏、分块摘要和短 ID 复制。
- [x] 优化 GitLab revision drift、向导检查清单、Secret 反馈和危险操作确认。
- [x] 更新对应静态页面契约测试。

## 3. 首页与 Wiki

- [x] 将首页重构为运行总览、待处理事项、快速操作和项目状态。
- [x] Wiki 接入统一外壳，删除固定悬浮管理按钮。
- [x] 为 Wiki 增加可折叠目录、窄屏证据抽屉和移动返回操作。
- [x] 保持并验证跨页面 `projectId/version` 深链。
- [x] 更新页面契约测试和空/错状态。
- [x] 从核心导航与首页移除版本比较入口，保留旧路由/API 兼容。

## 4. 代码工作台

- [x] 将代码控制区、图画布和信息栏统一为浅色工作区。
- [x] 将首屏控制重组为模式、查询、图谱选项三组。
- [x] 将高级筛选、图例和自动识别迁入合适的折叠控件。
- [x] 稳定右侧“信息 / 文件 / 证据”标签。
- [x] 接入共享错误规范化和诊断详情。
- [x] 增加移动端全屏 Tab，确保图谱与侧栏互斥。
- [x] 在不改业务行为的前提下拆出可复用的页面脚本/样式。
- [x] 保留并扩展 Monitor 页面契约测试。

## 5. 文档与验证

- [x] 将 Maven、应用信息、README 和 Changelog 统一升级为 `0.9.0`。
- [x] 更新用户指南、手工冒烟文档和 `CHANGELOG.md`。
- [x] 运行所有共享及页面静态契约测试。
- [x] 对全部共享/页面 JavaScript 运行 `node --check`。
- [x] 使用 JDK 21 运行 `./mvnw -B verify`。
- [x] 运行 `git diff --check`。
- [x] 完成 1440x900、1280x720、390x844 浏览器视觉与交互验收。
- [x] 确认生成 `target/NEXUS-0.9.0.jar`，并保留历史 `0.8.6` 评测基线标识。

## 风险与回滚点

- 不修改检索、索引、Wiki、版本比较和 GitLab 核心 API。
- 不一次性替换 `monitor.html`，按外壳、错误、布局、移动端逐块验证。
- 保持 `nexusApiKey` 存储键兼容，不将凭据写入 URL。
- 每迁移一组页面先运行对应契约测试，出现回归时只回退该页布局层。
- 保留上一任务所有未提交实现，不修改 `面试问答全解-NEXUS.md` 和本地数据库。

## 运行时修复

- [x] 监控统计使用不初始化 collection 的快速只读计数。
- [x] 监控页轮询禁止重入，并在 Qdrant DOWN 时降频到 15 秒。
- [x] 共享 API 客户端按大小写无关语义去重 Content-Type 和 X-API-Key。
