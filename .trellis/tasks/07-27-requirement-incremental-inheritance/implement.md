# Implementation Plan

1. 扩展需求快照条目模型，增加向后兼容的 `UPSERT/REMOVE` 操作。
2. 在快照仓库实现按基线链递归合成完整需求视图，并校验缺失基线、跨文档引用和循环。
3. 修改需求差异服务：快照走完整视图；Qdrant 回退不再按缺席推断删除。
4. 修改快照生成工具，仅从结构化操作列识别精确删除状态，不扫描正文。
5. 增加仓库和差异服务测试，覆盖继承、修改、显式删除、普通删除词、缺失基线、循环及真实 `5.0 -> 5.1` 数据。
6. 更新 README、CHANGELOG、后端规范和项目补丁版本。
7. 使用 Java 21 执行完整测试，运行生成器可重复性检查和 `git diff --check`。
